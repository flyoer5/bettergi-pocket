<resources xmlns:tools="http://schemas.android.com/tools">
    <!-- Base application theme. -->
    <style name="Base.Theme.BetterGIPocket" parent="Theme.Material3.DayNight.NoActionBar">
        <!-- Customize your light theme here. -->
        <!-- <item name="colorPrimary">@color/my_light_primary</item> -->
    </style>

    <style name="Theme.BetterGIPocket" parent="Base.Theme.BetterGIPocket" />

    <style name="Theme.BetterGIPocket.Home" parent="Theme.BetterGIPocket">
        <item name="android:windowBackground">@color/overlay_panel</item>
        <item name="android:statusBarColor">@color/overlay_panel</item>
        <item name="android:navigationBarColor">@color/overlay_panel</item>
    </style>

    <style name="Theme.BetterGIPocket.Transparent" parent="Theme.BetterGIPocket">
        <item name="android:windowIsTranslucent">true</item>
        <item name="android:windowBackground">@android:color/transparent</item>
        <item name="android:windowNoTitle">true</item>
        <item name="android:backgroundDimEnabled">false</item>
        <item name="android:statusBarColor">@android:color/transparent</item>
        <item name="android:navigationBarColor">@android:color/transparent</item>
        <item name="android:windowAnimationStyle">@null</item>
    </style>
</resources>